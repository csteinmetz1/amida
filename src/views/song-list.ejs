<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>amida | Songs</title>
	<link rel="stylesheet" href="css/styles.css">
	<script src="https://cdnjs.cloudflare.com/ajax/libs/tone/13.7.4/Tone.js"></script>

</head>

<body>
	<div style="margin: 15px 0 10px 0;"">
		<a class="home-link" href="/">
			<h1>amida</h1>
			<p>You've mixed <%= data.nMixes %> songs out of <%= data.songs.length %> </p>
		</a>
	</div>
    <div class="song-list fade-in one">
	<h2>Songs</h2>
		<div>
			<ul class="list-group">
				<% for(var i=0; i<data.songs.length; i++) {%>
					<li class="list-group-item">
						<a href="/mixer/<%= data.songs[i].id %>"> 
							<% if (data.mixes[i]) { %>
								<strong><%= data.songs[i].id %> - <%= data.songs[i].artist %> - <%= data.songs[i].title %> </strong>
							<% } else { %>
								<%= data.songs[i].id %> - <%= data.songs[i].artist %> - <%= data.songs[i].title %> 
							<% } %>
						</a>
					</li>
				<% } %>
			</ul>
		</div>
    </div>
</body>

</html>