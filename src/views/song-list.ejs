<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>amida | Songs</title>
	<link rel="stylesheet" href="css/styles.css">
	<script src="https://cdnjs.cloudflare.com/ajax/libs/tone/13.7.4/Tone.js"></script>

</head>

<body>
    <section class="home">
	<h2>Songs</h2>
		<div>
			<ul class="list-group">
				<% for(var i=0; i<songs.length; i++) {%>
					<li class="list-group-item">
						<a href="/mixer/<%= songs[i].id %>"> <%= songs[i].id %> - <%= songs[i].artist %> - <%= songs[i].title %> </a>
					</li>
				<% } %>
			</ul>
		</div>
    </section>
</body>

</html>