<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>amida | Songs</title>
	<link rel="stylesheet" href="css/styles.css">
	<script src="https://cdnjs.cloudflare.com/ajax/libs/tone/13.7.4/Tone.js"></script>

</head>

<body>
	<div style="margin: 15px 0 10px 0;">
		<p style="display: inline;"> Logged in as <strong><%= data.userId %></strong></p>
		<button class="save-button" type="btn" onclick="location.href = '/logout';"">
			<p>Logout</p>
		</button>
	</div>
    <div class="song-list">
	<h2>Songs</h2>
		<div>
			<ul class="list-group">
				<% for(var i=0; i<data.songs.length; i++) {%>
					<li class="list-group-item">
						<a href="/mixer/<%= data.songs[i].id %>"> <%= data.songs[i].id %> - <%= data.songs[i].artist %> - <%= data.songs[i].title %> </a>
					</li>
				<% } %>
			</ul>
		</div>
    </div>
</body>

</html>